1)Node JS:
*Node dosen't have a predefined "window" object cause it doesn't have a window to draw anything'
  *"location" object is related to a particular url'
  *Node has "global" which is a predefined global object.
  *Required object is predefinedin Node which is used to includes modules in the app.
 Browser JS:
   * “window” is a predefined global object which has functions and attributes
   *“location” is another predefined object in browsers, that has all the information about the url we have loaded
   *“document”, which is also another predefined global variable in browsers, has the html which is rendered.
   
 2) * HTML tree 
    *DOM + CSSOM
    *DOM Node to render object
    *inline critical CSS
    *DOM TREE -> RENDER TREE

 4) a) Typeof(1) - console.log(typeof 42); Its an number.
    b) typeof(1.1) - 'number'
    d) typeof(true) - 'boolean'
    e) typeof(null) - 'object'
    f) typeof(undefined) - 'undefined'
    g) typeof([]) - 'object'
    h) typeof ({}) - 'function'
    i) typeof (NAN) - 'number';//Despite being 'Not a number'
